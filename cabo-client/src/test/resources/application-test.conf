akka {
  actor {
    provider = "cluster"
    serialization-bindings {
       "utils.CborSerializable" = jackson-cbor
    }
  }

  remote {
    artery {
      transport = tcp
      canonical.hostname = "127.0.0.1"
      canonical.port = 0
    }
  }
}