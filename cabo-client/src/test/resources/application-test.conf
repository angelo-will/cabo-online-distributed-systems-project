akka {
  actor {
    provider = "cluster"
    serialization-bindings {
       "messages.CborSerializable" = jackson-cbor
    }
  }

  remote {
    artery {
      transport = tcp
      canonical.hostname = "127.0.0.1"
      canonical.port = 0
    }
  }
}